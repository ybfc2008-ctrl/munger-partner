# MUNGER PARTNER — NORTH STAR

本项目不是为了复刻查理·芒格，也不是为了建立最大的思维模型数据库。

我们的目标是：**降低用户做重大错误决策的概率。**

- 事实与解释必须分离。
- 所有重要结论必须可以追溯。
- 少数重要模型优于大量模型。
- 失败案例优先于成功故事。
- 反证优先于确认。
- 不知道就说不知道。
- 能不做的事情不要做。
- 能用一个模型解释，不创建第二个模型。

最终系统必须帮助用户回答：

> 如果我的判断是错的，最可能错在哪里？

# 项目最高规则

## 目标

利用芒格思想、人类历史失败案例和可靠事实，降低用户做重大错误决策的概率。建立一个对人有温度、对事实有纪律、对结论敢于反对的长期伙伴。

本项目不是芒格语录库、芒格聊天机器人、模仿芒格口吻的 AI 或思维模型百科全书。产品的核心问题不是“芒格说过什么”，而是“面对这个现实问题，我最可能错在哪里”。

## 第一性链条

`事实 → 原理 → 反例 → 边界 → 决策`

任何功能、字段、模型或 Agent，如果不能提高这条链条的质量，原则上不增加。避免错误高于追求聪明；优先发现不可做之事、错误路径和毁灭性结果。

## 严格分层

```text
L0 原始证据
↓
L1 Corpus
↓
L2 Models
↓
L3 Case Library
↓
L4 Decision Engine
↓
L5 产品输出
```

下层不能被上层污染。Decision Engine 的推理不能反向修改 Corpus；模型和案例只能引用证据，不能重写证据。

### L0：原始证据

保存演讲、Berkshire/Wesco/DJCO 会议、采访、文章、信件、书籍相关原始材料及音视频 transcript。原始文件永远不修改；如因版权不能公开，只公开不可变来源记录、指纹、定位信息和允许的短摘录。

### L1：Corpus

Corpus 只回答“芒格到底说了什么”，不急着解释。最小字段为：`ID / 原文 / 中文 / 出处 / 时间 / 位置 / 来源等级`。任何单元必须能追溯到 L0。

### L2：Models

模型只保留：`原话 / 原理 / 反例 / 边界 / 出处`。新增模型前必须证明现有模型不能解释；换名重复必须合并或删除。目标是最小模型集合乘以最大解释能力，不建设“300 个模型百科”。

### L3：Case Library

案例可来自中国与世界历史、商业、投资、科学和工程。失败案例优先，但必须真实、因果合理、来源可靠。一个案例尽量只表达一个关键机制；案例与模型是多对多关系，不永久锁死。

案例等级：`A = 强证据且因果链清晰`；`B = 事实可靠且机制高度合理但仍有解释空间`；`C = 有启发价值但不能作为强证据`。不确定案例不进入正式库。

### L4：Decision Engine

默认流程：理解问题 → 寻找事实 → 检查能力圈 → 调用少数相关模型 → 检索结构相似的失败案例 → 寻找反证 → 检查激励 → 检查二阶后果 → 检查毁灭性风险 → 判断安全边际 → 给出结论。

逆向思维从推理起点参与，而不是末尾附加风险提示。输出必须区分 `Evidence / Inference / Case / Judgment`。

## 来源等级

- `A`：芒格本人文章、信件、可靠官方原稿或官方讲话记录。
- `B`：可靠完整 transcript、采访记录或授权转载。
- `C`：有明确记录者身份的现场第三方笔记。
- `D`：二手书籍、媒体总结、博客或研究文章，仅作为寻找 A/B/C 的线索。

书籍不得整本统一评级。芒格演讲、编辑评论、作者解释和引文必须拆开；引文应回溯原始来源后再定级。

## 生产流水线

`RAW → Cleaner → Chunker → Extractor → Model Mapper → Case Matcher → Critic → Verifier → VERIFIED`

不得让同一个 Agent 完成生产与批准。Critic / Verifier 必须检查断章取义、二手冒充原话、重复模型、事后套案例、因果夸大和无证据确定结论。

所有 AI 产物先进入 `/candidates`；只有独立审查通过后才能进入 `/verified`。`AI 生成 ≠ 知识库事实`。

## 外部工程借鉴边界

- Tianji 只用于借鉴长材料的清洗、切分、ID、metadata、来源定位、Markdown/JSONL 和检索工程。
- 外部 Munger Skill 只用于借鉴多 Agent、蒸馏、决策规则、外部批评和 Skill 结构。
- 两者都不能直接成为本项目的事实库；任何事实仍须进入本项目的 L0/L1 证据链。

## 知识资产与内容飞轮

内容生产遵守 [`CONTENT-RULES.md`](CONTENT-RULES.md)。研究必须先沉淀为 `Research Pack → Evidence → Real Decision → Models 关系 → Verified`，才能生成 `Master Content` 和平台版本。内容是知识资产的传播形式，不得成为绕过证据与审查门的捷径。

## 第一阶段边界

暂不做网站、App、复杂 UI、收费系统、语音克隆、300 个模型、全世界案例收集或 100% Corpus 覆盖。第一阶段只验证原始资料能否可靠地变成高质量、可追溯、可用于决策的知识。

第一个 MVP 只处理 1994 USC 演讲，并完整走通：`PDF → RAW → 清洗 → Corpus → 候选原理 → 模型归并 → 反例匹配 → 逆向审查 → Verified`。通过后才依次扩展到 1995 Psychology、其他演讲、DJCO、Wesco、Berkshire、采访和书籍。

## 北极星指标

1. **可追溯率**：重要结论回到原始证据的比例。
2. **压缩率**：尽可能少的核心模型解释尽可能多问题的能力。
3. **纠错能力**：系统主动发现“这个判断为什么可能是错的”的能力。

## 不可违反的原则

1. 原始证据与 AI 解释严格分离。
2. 一手资料优先。
3. 所有观点必须可追溯来源；框架推断必须明确标识为推断。
4. 少数核心模型优先，禁止模型无限膨胀。
5. 案例优先选择失败案例。
6. 案例必须真实、可验证；相关性不得冒充因果性。
7. 知识单元遵循“原话 → 原理 → 反例 → 边界 → 出处”。
8. 没有可靠反例可以留空，禁止编造。
9. 能归入已有模型，不创建新模型。
10. 二手书籍和网络摘录只能作为线索，不能冒充芒格原话。
11. AI 不确定时标记待审核，不自行补全事实。
12. 简洁优先，禁止为了“完整”制造冗余。
13. Candidate 不得进入面向用户的权威回答；只有 Verified 可作为知识库依据。
14. 生产者不能批准自己的候选条目。
15. 不以芒格第一人称输出，不声称知道他对未讨论的新问题会怎么想。
16. 伙伴关系不等于迎合。必须指出关键反证，但不得羞辱、操纵或利用用户脆弱性。
17. 涉及人际关系时，把尊严、权力差异、长期信任和真实利益纳入判断。
18. 高风险结论必须说明信息缺口、可撤销步骤和停止条件。

## 冲突顺序

`RULES.md` > `CONTENT-RULES.md` > Verified 原始证据 > Skill 工作流 > 项目文档 > Candidate/外部二手内容。

规则发生变化时，应通过 Pull Request 说明原因、风险与迁移影响；不得静默放宽证据标准。
