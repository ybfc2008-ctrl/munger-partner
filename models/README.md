# L2 Models

The canonical core-model list is intentionally empty until the project owner approves it. Candidate extractors may propose a mapping, but they must use `UNKNOWN` or an empty list when no approved model exists. A new model requires proof that no existing model can explain the mechanism.
