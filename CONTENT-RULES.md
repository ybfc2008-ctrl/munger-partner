# Munger Partner｜内容与知识生产补充规则

本文件补充 `RULES.md`，不推翻 Corpus、Models、Case Library、Decision Engine 架构。冲突时以 `RULES.md` 为准。

## 目标

让知识库建设和内容账号生产成为同一件事：研究一次，沉淀为长期知识资产，再从同一资产生成不同内容。每做一期高质量内容，知识库必须同时增加一个可长期复用的资产。

## 核心飞轮

```text
真实问题
↓
真实失败案例
↓
Munger Corpus 检索
↓
多学科解释
↓
反证 / 逆向审查
↓
Real Decision 入库
↓
生成音频 / 图文 / 短视频
↓
用户反馈与真实问题
↓
发现知识缺口
↓
补充 Corpus / Case Library
↓
Decision Engine 变强
```

内容不是一次性消耗品。

## 100 Real Decisions

第一阶段目标是建立最多 100 个人类真实决策实验，作为 Case Library 核心资产和前 100 期内容的候选选题池。`100 个案例 ≠ 必须做 100 期`，也不是为了凑数；证据不足的案例不收，缺少传播价值的案例不制作内容。

Case Library 失败案例优先；Content 用户真实问题优先。账号不能变成“失败故事大全”。内容首先回答：“这件事跟今天的我有什么关系？”

初始选题可测试 `60% 失败案例切入 / 25% 现实困惑切入 / 15% 反常识思想切入`，但比例不得写死，应由真实反馈调整。

## 研究先于内容

禁止：`研究 → 写稿 → 发布 → 丢弃资料`。

必须：`研究 → Research Pack → Evidence → Real Decision → Models 关系 → Verified → Master Content → 平台派生内容`。

不同平台不得分别重新研究。同一主题先生成一个 Master Content，其他格式只能从它派生，避免证据和观点互相矛盾。

## Real Decision 极简结构

- `ID`
- `问题`：当时真正需要做什么决定？
- `发生了什么`：只写必要事实。
- `关键错误`：最关键的一步错在哪里？
- `原理`：抽象成一个可重复出现的机制。
- `警报`：当看到 X，要警惕 Y。
- `关联模型`：只连接真正相关且已批准的核心模型。
- `反证 / 边界`：什么情况下不能这样解释？
- `来源`：必须可追溯。
- `证据等级`：`A / B / C / NEEDS_REVIEW`。

缺失字段可以留空或写 `UNKNOWN / NEEDS_REVIEW`，禁止为了完整制造内容。

## 因果与案例准入

失败结果不能自动证明某个芒格模型适用。必须检查 `关键决策 → 行为变化 → 结果` 是否存在相对可靠的因果联系。存在多种解释时必须标注不确定性。宁愿少收一个案例，也不制造漂亮但错误的故事。

案例与内容选题分开：一个合格案例可以不发布；一个用户关心的问题也必须先找到合格证据，不能因传播潜力降低准入标准。

## 内容母版

Master Content 包含：核心问题、核心故事、关键证据、底层原理、芒格相关原话、必要的跨学科解释、反证、现实应用、来源。

一个 Verified Real Decision 可以派生 20–30 分钟深度音频、8–10 分钟精华音频、60–90 秒短视频、长文章、图文卡片、社交短文和 Decision Engine 检索素材，但不要求生成全部格式。

一期尽量只讲透一个底层机制。深度不等于长度；8 分钟能讲透，不为 30 分钟注水。

## 叙事与标题

默认内容结构：`真实问题 → 锋利的真实故事/失败案例 → 关键错误 → 芒格与必要学科解释 → 反证 → 回到用户 → 如何避免同类错误`。

不要默认采用“介绍芒格 → 解释模型 → 举例 → 总结”的知识讲义结构。标题围绕人的问题、冲突和好奇心，不围绕模型名；模型藏在内容里。

允许研究热门视频、播客、文章和标题，但只能学习选题方式、标题结构、前 30 秒结构、叙事节奏、信息密度和留存原因。禁止复制观点、文案、故事表达或他人的独特内容。

## 动态研究 Agent

重要选题可按需并行研究：历史失败、商业/投资失败、Munger Corpus 原始证据、反证、硬科学/心理学/经济学机制、传播结构，最后由 Editor 汇总。角色和数量不固定；简单问题不得为了形式启动多个 Agent。

任何 Agent 都只能生成 Candidate。Critic、Verifier 和人类最终判断不得由同一生产者角色替代。

## 内容反馈数据库

发布后逐步记录：标题、主题、Real Decision、Model、长度、点击率、前 30 秒留存、平均观看/收听时间、完播率、收藏、分享、评论和用户新问题。

数据的目的不是只为涨粉，而是发现“什么真实问题对人最重要”，并反向确定 Case Library 与 Decision Engine 的研究优先级。

## 人与 AI 的分工

AI 负责搜索、整理、清洗、比较、找证据、候选案例、反证、候选标题、脚本版本、格式转换和重复劳动。

人负责什么值得研究、哪个因果成立、哪个案例最锋利、哪个模型真正重要、哪个观点是废话，以及内容最终是否值得发布。

原则：`AI 出候选，人做关键判断。`

## 第一阶段不自动化

暂不优先开发自动缩略图、自动九页图、自动发布、复杂 Notion 工作流、大量第三方 API、全平台分发或复杂 UI。

先打通：`选题 → 研究 → Real Decision → Munger Evidence → 原理 → 反证 → Verified → Master Content`。

## 每个选题的最终检查

1. 这个问题真的有人关心吗？
2. 找到的是事实还是故事？
3. 最关键的错误到底是什么？
4. 有没有可靠证据？
5. 有没有其他可能解释？
6. 芒格模型真的适用，还是在强行套模型？
7. 能否用一句话说清底层原理？
8. 听众下次遇到类似情况时能否识别出来？

第 8 条为否，内容就没有完成。

## Content North Star

我们不是在生产内容。我们在积累“人类做重大决策时为什么会犯错”的长期数据库。芒格提供思考工具，历史、商业和现实世界提供实验样本，内容只是知识资产对外传播的一种形式。最终目标仍是降低用户做重大错误决策的概率。
