# 贡献指南

感谢你帮助这个伙伴变得更可信，而不是只是更像芒格。

## 提交知识单元

1. 阅读 `RULES.md`。
2. 从 `templates/knowledge-unit.md` 创建条目，状态必须是 `candidate`。
3. 附上可访问来源和精确定位；不要只给搜索结果、语录图片或 AI 摘要。
4. 运行 `python3 scripts/validate_corpus.py`。
5. Pull Request 中说明它应归入哪个已有模型，以及最强反证是什么。

Candidate 的作者不得同时作为最终审查者。维护者核对原始来源后，才能把状态和路径改为 `verified`。

## 纠错优先

以下贡献优先于新增模型：错误归因、断章取义、重复条目、翻译偏差、伪因果、缺失边界、过期事实。来源被撤回或不可复查时，Verified 应降级回 Candidate，而不是保留“看起来正确”的结论。

## Pull Request 最低要求

- 变更范围清楚且尽量小。
- 新规则说明它防止的具体失败。
- 新模型证明不能归入既有模型。
- 新代码包含可观察的验证方式。
- 不提交受限版权全文、密钥、个人隐私或未经同意的私人对话。

## 行为准则

讨论观点，不攻击贡献者。来源争议以可复查证据为中心；承认不确定性不会降低贡献价值。
